# A builtin function of list are called.

my_list = [1, 2]; my_list_0=my_list[0]; my_list_1=my_list[1]

length = len(my_list)

total = sum(my_list)

largest = max(my_list)

smallest = min(my_list)

sorted_list = sorted(my_list)

any_list = any([1, True])

all_list = all([1, True])
